[phases.setup]
nixPkgs = ["jdk21", "maven"]

[phases.build]
cmds = ["mvn clean package -DskipTests -B"]

[start]
cmd = "java -jar target/ssms-predictive-module-0.0.1-SNAPSHOT.jar --server.port=$PORT"
